%table
  %tr
    %th Name
    %th Project
    %th This day
    %th Total
    %th Estimation
    %th Status    
    %th Action

  - tasks_by_dates.each_pair do |date, tasks|
    %tr.date
      %td= date.to_s :human
    - tasks.each do |task|
      %tr
        %td= link_to task.name, task
        %td= link_to task.project.name, task.project
        %td= task.duration_on_date date
        %td= task.duration_total
        %td= task.estimation
        %td= task.status
        %td
          = interrupt_link(task) if task.running?
          = resume_link(task) if task.archived? || !task.running?
          = archive_link(task) unless task.archived?

        
          